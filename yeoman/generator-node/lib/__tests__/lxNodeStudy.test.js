const assert = require('assert');
const lxNodeStudy = require('../index.js');

describe('lxNodeStudy', () => {
  it('has a test', () => {
    assert(false, 'lxNodeStudy should have a test');
  });
});
